import React, { Component } from 'react';
import './Select.css';

export default class Select extends Component {
  render() {
    return (
      <select>
        <option>1</option>
        <option>2</option>
      </select>
    );
  }
}
